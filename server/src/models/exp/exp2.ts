switch (k) {
  case 'left': {
    worldMove(aux.neg1, 0, w)
    break
  }
  default: {
    // do nothing
    a = 2
    break
  }
}

// if (k === 'left') {
//   worldMove(aux.neg1, 0, w)
// } else {
//   // do nothing
//   //     break
//   a = 2
// }
