/**
 * Type to represent the user details
 * Contains the user id, first name, last name, email, kind of role etc.
 */
export type UserDetails = {
  id: number
  firstName: string
  lastName: string
  institution: string
  email: string
  role: number
}
