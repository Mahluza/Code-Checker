/**
 * Enum to represent the type of match
 * CommonLines represents that few lines matched
 * CompleteMatch represents that the code blocks have completely matched
 */
export enum MatchType {
  CommonLines = 'COMMON_LINES',
  CompleteMatch = 'COMPLETE_MATCH',
}
